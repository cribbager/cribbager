node_modules/
.claude/settings.local.json
/bin/

# go build outputs dropped in the repo root
/cribbager
/cribbager-server

# misc
*.log
.DS_Store

# local secrets (e.g. DATABASE_URL) — never commit
.env
.env.*

# research notes kept locally for now, not published — EXCEPT the ML bot
# program (docs/research/ml-bot), published as a tutorial narrative. The `*`
# form matters: ignoring the directory itself would prevent re-including any
# child.
docs/research/*
!docs/research/ml-bot/
/mldata
/pegdata
